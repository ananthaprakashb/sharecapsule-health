import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'org.sharecapsule.vital',
  appName: 'Vital by ShareCapsule',
  webDir: 'dist',
}

export default config
